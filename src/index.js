import React from 'react';
import ReactDOM from 'react-dom/client';
import DsApp from './DS/DsApp.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DsApp/>
  </React.StrictMode>
);